# Build the Discord bot image (context = repo root).
substitutions:
  _IMAGE: us-east1-docker.pkg.dev/war-bot-476522/cloud-run-source-deploy/war-bot:latest

steps:
  - name: gcr.io/cloud-builders/docker
    args: ["build", "-f", "Dockerfile", "-t", "${_IMAGE}", "."]

images:
  - ${_IMAGE}
